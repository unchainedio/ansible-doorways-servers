---
- name: Ensure MySQL is installed
  hosts: all
  become: yes

  tasks:
    - name: Update the apt package index
      ansible.builtin.apt:
        update_cache: yes

    - name: Install MySQL server
      ansible.builtin.apt:
        name: mysql-server
        state: present
        force_apt_get: yes
